import { CategorieIngredient } from './categorie-ingredient';

describe('CategorieIngredient', () => {
  it('should create an instance', () => {
    expect(new CategorieIngredient()).toBeTruthy();
  });
});
