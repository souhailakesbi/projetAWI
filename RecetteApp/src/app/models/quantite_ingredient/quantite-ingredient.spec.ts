import { QuantiteIngredient } from './quantite-ingredient';

describe('QuantiteIngredient', () => {
  it('should create an instance', () => {
    expect(new QuantiteIngredient()).toBeTruthy();
  });
});
